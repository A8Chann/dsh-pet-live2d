docs(README): 同步槽位表与装扮契约；顺带修掉两处过期的槽位数断言

README（插件 + 示例宠物两份）：
- 槽位表按当前 pet.json 重新生成（17 个槽位：手部拆成左右手、鲸鱼独立、点菜独立）
- 新增「装扮：穿在身上的东西」小节：会话相位不动、归位不清、跨启动从
  localStorage 穿回来，以及为什么 commitPinsRef 要最后合并装扮 pins
- 拖动那一行补上「可以压出屏幕底边」

driver：cdp-merge / cdp-idle-return 里 slots === 16 是硬编码的，拆成 17 之后两个
都红了。cdp-idle-return 还顺带暴露一件事：它判定「离开相位清空表达式」用的是数量
归零，而用户挑的装扮现在**应该**留着 —— 改成判定相位自己钉的那几个表达式，并把
「相位确实钉上了自己的表达式」也改成按名字判定，免得装扮常驻之后这条变成空洞成立。

验证：cdp-merge 15/15、cdp-idle-return 11/11
（相位中 [头顶鲸,画笔,圆眼镜] -> 离开后 [圆眼镜]）。